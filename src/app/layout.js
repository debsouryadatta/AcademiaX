// src/app/layout.js
import React from 'react';

const Layout = ({ children }) => {
  return (
    <div>
      {/* Your layout structure */}
      {children}
    </div>
  );
};

export default Layout;
